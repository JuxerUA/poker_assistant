import 'package:flutter/material.dart';

class BackToGameWidget extends StatelessWidget {
  const BackToGameWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
